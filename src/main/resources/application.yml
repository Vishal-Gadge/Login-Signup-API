server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

spring:
  data:
    redis:
      host: localhost
      port: 6379
      time-to-live: 600000

  datasource:
    url: jdbc:mysql://localhost:3333/security
    username: root
    password: DangerArmy@3333

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: vishaldangerarmy1@gmail.com
    password: wgsiugkxnvgdigtg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000